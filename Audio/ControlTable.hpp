/**
 * @ Author: Pierre Veysseyre
 * @ Description: ControlTable.hpp
 */

#pragma once

#include <Audio/Control.hpp>

namespace Audio
{
    struct ControlTable;
};

struct Audio::ControlTable
{

    // template<bool EnableControl = false>
    static void GenerateControl(const ControlEvents &controls) {

    }
};